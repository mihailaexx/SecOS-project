---
- name: daemon reload idp
  become: true
  become_user: secos
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ secos_uid }}"
  systemd:
    daemon_reload: true
    scope: user

- name: restart idp
  become: true
  become_user: secos
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ secos_uid }}"
  systemd:
    name: secos-idp
    state: restarted
    scope: user

- name: reload firewalld
  systemd:
    name: firewalld
    state: reloaded
