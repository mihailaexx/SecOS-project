---
- name: daemon reload
  systemd:
    daemon_reload: true

- name: restart idp
  systemd:
    name: secos-idp
    state: restarted

- name: reload firewalld
  systemd:
    name: firewalld
    state: reloaded
